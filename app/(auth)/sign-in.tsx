import React from "react";
import { ScrollView, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

function SignIn() {
  return (
    <SafeAreaView>
      <ScrollView className="flex-1 bg-pink"></ScrollView>
    </SafeAreaView>
  );
}

export default SignIn;
